# Part-2-SQL-Query-Implementation-Verification
Objective In this part, you will write selected SQL queries on top of your database and verify that the outputs are logically correct.  This part tests practical SQL querying ability, but marks are awarded not only for writing queries, but also for validating outputs using the actual dataset.
